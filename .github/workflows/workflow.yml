name: CI workflow

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    name: Build Job (Task 1)
    runs-on: ubuntu-latest
    container: python:3.9-slim
